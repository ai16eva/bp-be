# moonplay-api
An API for moonplay

# Run
``npm install``

``npm start``
# Generate API DOC
In order to generate the api documentation run the following

``npm run swagger-autogen``

Then go to `/api-docs` to access it
 
