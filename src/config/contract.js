require('dotenv').config();

module.exports = {
  dev: {
    // Legacy EVM config removed; keep file to avoid widespread imports breakage
  },
  test: {
    // Legacy EVM config removed
  },
  prod: {
    // Legacy EVM config removed
  },
};
